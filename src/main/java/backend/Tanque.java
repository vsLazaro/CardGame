package backend;
import abstracts.ACartaTropa;

public class Tanque extends ACartaTropa {
    public Tanque(String nome, String tipo, String dirImage, int vida, int dano) {
        super(nome, tipo, dirImage, vida, dano);
    }
}
